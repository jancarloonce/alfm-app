rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow all reads and writes (single user app, no auth needed)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
